Building instructions:
---------------------

Run:

cd zoekmuis
make

Run the webspider by going into the same directory and type
./webspider http://my.url.com/

The webserver/interface can be launched by:
cd mongoose
./mongoose

Now visit http://localhost:8080
